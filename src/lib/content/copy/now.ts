import type { NowCopy } from './now.types';

export const nowCopy: NowCopy = {
	heading: 'Now',
	intro:
		"What I'm actually spending time on right now, not a running list of everything I've ever done — that's what /work and /about are for. Updated by hand, occasionally.",
	updatedLabel: 'Last updated'
};
