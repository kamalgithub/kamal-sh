import type { CtaCopy } from './cta.types';

// Reused as the closing call-to-action across pages — one shared banner, not duplicated per page.
export const ctaCopy: CtaCopy = {
	heading: "Let's build together",
	body: "Whether it's a cloud migration, a DevOps transformation, or just a technical discussion — I'm always open to connecting.",
	buttonLabel: 'Get in touch'
};
