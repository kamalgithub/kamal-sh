import type { PostmortemsCopy } from './postmortems.types';

export const postmortemsCopy: PostmortemsCopy = {
	heading: 'Postmortems',
	intro:
		'A public, blameless record of real incidents from kamal.sh and Aicademy — what broke, why, and what changed as a result.',
	emptyStateMessage:
		'Nothing has broken publicly yet. This page exists so that when something does, it gets written up here instead of quietly patched and forgotten.'
};
