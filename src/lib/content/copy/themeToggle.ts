import type { ThemeToggleCopy } from './themeToggle.types';

export const themeToggleCopy: ThemeToggleCopy = {
	lightLabel: 'Light',
	systemLabel: 'System',
	darkLabel: 'Dark',
	triggerLabelTemplate: 'Change theme (currently {theme})'
};
