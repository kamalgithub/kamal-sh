import type { Postmortem } from './postmortems.types';

// Intentionally empty — see copy/postmortems.ts's emptyStateMessage for why. Add a real
// entry here the first time something on kamal.sh or Aicademy actually breaks publicly.
export const postmortems: Postmortem[] = [];
